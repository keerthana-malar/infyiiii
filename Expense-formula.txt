name = createdBy.userName;
balance = user.userbalance;

ifThen(
    status == 'approved' && final == true,
    balance = user.userbalance - amount;
    record\update('User', createdById, 'userbalance', balance)
    );
